// Configuration file for account status
const config = {
  // Set your account status here
  // Options: "termed", "new", "limited"
  accountStatus: "limited",

  // Custom status text (optional)
  // If not provided, default text will be used based on status
  customStatusText: "",
}
